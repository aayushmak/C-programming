#include <stdio.h>

#define PI 3.14159 // simple macro

int main() {
    float radius = 5.0;
    float area = PI * radius * radius;

    printf("Radius = %f\n", radius);
    printf("Area = %f\n", area);
    return 0;
}

